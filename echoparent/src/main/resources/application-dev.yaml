spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/echomusic
    username: postgres
    password: 131420
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      password: 131420

# 阿里云 OSS：在 application.yaml 的 aliyun.oss 中通过环境变量 ALIYUN_OSS_* 填写，勿将密钥写入仓库。
